---
import { Image } from "astro:assets";

export type NavigationProps = {
  links: {
    iconUrl: string;
    url: string;
    label: string;
  }[];
};

const { links } = Astro.props as NavigationProps;
---

<ul class="flex justify-center gap-3 lg:justify-normal">
  {
    links.map((link) => (
      <li>
        <a href={link.url}>
          <Image
            class="h-6 w-6 invert"
            src={link.iconUrl}
            alt={link.label}
            width="24"
            height="24"
            title={link.label}
          />
        </a>
      </li>
    ))
  }
</ul>
